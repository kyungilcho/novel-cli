[package]
name = "novel-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
novel-core = { path = "../../crates/core" }

[dev-dependencies]
assert_cmd = "2"
predicates = "3"
tempfile = "3"
diesel = { version = "2.2", features = ["sqlite"] }
